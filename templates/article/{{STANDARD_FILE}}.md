# {{ARTICLE_TITLE}}—the standard

PLACEHOLDER—replace this paragraph with the body of the standard itself. Rename this file to the name your article uses for it and change the matching `{{STANDARD_FILE}}` entry in `SUMMARY.md`; both are `{{...}}` tokens, so a search for the opening braces finds every one left. Do not publish the placeholder.
