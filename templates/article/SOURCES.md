# Sources

PLACEHOLDER—replace this paragraph with the register of every external source the article's claims rest on: one list item per source, giving its URL, the date it was last read, and the claim it supports. The monthly `watch-sources` workflow runs a `tools/watch-sources.mjs` that you write for this repository (the skeleton ships the workflow, never the script), so keep the format that script reads.
