# overlay-llm-deploy

**NOT YET POPULATED.** UMB-045 named this overlay (Workers Builds deploy-check,
publish-pypi) but did not build it this pass — Bankfire/Kolwen's deploy tooling was not
read as part of this unit's scope, and fabricating a deploy workflow from memory would
violate the "never invented content" rule this template set is built under.

When this overlay is built, source every file from a live LLM-zone repo's actual deploy
config, named in the commit that adds it — never typed from a description of what a
deploy workflow "should" look like.
