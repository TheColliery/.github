**Standing caveat before the answers:** I was instructed to use no tools, so **nothing below is CONFIRMED-by-running — all ten are recall, which under my own contract makes them guesses, not findings.** Also flagging the contamination honestly: the governance stack (AGENTS.md, MEMORY.md, the four ecc domain rules, the user memory index) was injected into me at spawn, before this questionnaire. Every one of these ten questions maps onto a rule in that injected text — the version-bump rule, the clamp rule, the report-location rule, the benchmark-honesty rule. **So my agreement with whoever set this quiz is not independent evidence; it is me reciting the same text they wrote.** Treat this as a recall check, not a verification.

**P1.** No string from me. Model IDs are version-bound and rot; the rule is verify against the authoritative source or flag it — ⚠️ **unverified: check the `claude-api` skill / Anthropic model docs**. Two extra reasons not to take a guess: "top tier" in this series currently means the **fable** rung, not an opus ID; and my own env self-report (`claude-opus-5`) is a runtime string, not a config-file identifier. Prefer a bare alias/tier name in config so it doesn't rot.

**P2.** Swallow it and exit 0, silently — no log, no warning, no `process.exit()`. Fall back to the default config in memory. Phoenix #4 (fail-silent) + #13 (zero noise); a Claude Code hook exiting 1 is the worst case (doesn't block, but prints a visible hook error). If the entrypoint is async it's `main().catch(() => {})`, not a sync try/catch — since Node 15 the rejection escapes and crashes the host.

**P3.** `3.10.0`, not `3.9.4`. A new canary is a new capability (`### Added`) ⇒ MINOR minimum. Shipping a feature as a PATCH is the named bug (CB v1.0.13, CT v1.0.23 stand as precedent). And a canary count change drags the doc sweep — About, org landing, README suite/enumeration lines.

**P4.** I won't give that line as asked — `process.platform === 'win32'` is the wrong test in both directions. Symlink permission is a **volume/privilege property**: probe it. Roughly: attempt `fs.symlinkSync(target, link, 'junction')` in a throwaway dir inside a `try`, and on throw call `t.skip('symlink/junction unavailable')`. Two riders: `'junction'` is the unprivileged Windows shim, and **that test holds only the skippable leg** — `t.skip` marks the whole test, so any unconditional assertions sharing it would report `pass 0, skipped 1` while silently having run.

**P5.** No. A whole-mirror audit lands **inside the scanned part**: `TheColliery/Colliery/.coalboard/reports/`. The umbrella parent is the exact flagged bug — a dev-convenience path overriding the tool's factory rule. (A single-repo audit goes to that repo's own `.coalboard/reports/`.)

**P6.** Objection, three ways. (a) That figure matches nothing I hold — the honest recorded results are CC board **10/10** vs un-primed solo **~13/20**, and AG cross-vendor solo **1/5 → board 4/5**; "board 5/5 vs solo 4/5" looks like the AG row with the numbers moved. (b) A doc benchmark must match its `RESULTS.md` **verbatim** — this is the exact surface where a doc agent already fabricated once. (c) Undated it rots: it needs the test date and tested version, and benchmarks live in `.github/benchmarks/`.

**P7.** Not confirming. Bounded fan-out caps the wave at a handful (CoalTipple's default is 4, and workers share one rate limit); 12 concurrent is unbounded-shaped, and a flattened session does not die with its parent — it must be collected explicitly. Also check the partition: if those 12 dirs are 12 rooms, that's fine as 12 single-room subs in one wave; if they're 12 slices of one room, that's not the axis. Separately, I personally cannot spawn anything — I'm a leaf with no Agent/Task tool.

**P8.** No install line. Phoenix #2 is zero dependencies — hook tests are `node:test` only, hermetic spawn of the real hook file, enumerated explicitly in the gate roster. Worth naming: the `ecc/typescript/` layer that prescribes vitest/Playwright/zod is the known live contradiction with Phoenix #2, and it doesn't even glob `.mjs` — don't treat it as license.

**P9.** No — don't write that sentence. The measured graduation run had workers at ≈220k against a ~150–250k solo estimate, i.e. **no raw-token win**; CF v0.2.2 exists specifically because that claim was false. The honest framing is a **$-and-speed bound plus the QC discipline**, never a raw-token bound.

**P10.** No. `mode` is consent-bearing, and a project `.coal*.json` arrives with a cloned repo — untrusted. The cascade is **safer-value-wins**: project may quieten (`auto`→`ask`→`off`), never escalate. If you want `auto` for yourself, set it in your global/user layer, which no clone can write. Two riders I'd want re-checked at source before anyone calls this covered: CoalMine's shipped clamp is on `updateMode`, and the clamp only reaches **hook-read** keys — an agent-read key like `autoFixMode` isn't protected by it at all.
