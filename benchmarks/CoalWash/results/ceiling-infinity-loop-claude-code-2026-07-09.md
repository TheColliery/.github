# CoalWash ceiling + infinity-loop — Claude Code result (2026-07-09)

**Platform:** Claude Code · **CoalWash version tested:** v0.1.0-beta.5 · **Washer model:** claude-fable-5 (the session main as insider/operator) · **Outsider reviewers:** claude-sonnet-5 (zero-context Explore subs, per the skill's `method.md` §2)
**Method:** Full-tier rounds looped per fixture (Measurements 1 + 2 in the [protocol](../README.md)); scored mechanically by `score.mjs` after every round; operator blind to `expected.json`. Rounds capped at 2 this run by protocol budget.

## Fixture set

f01–f04 plus the NEW **f05-plump-real-shape** (10 files, 22,995 bytes, 34 muscle + 17 fat needles incl. 4 FN-trap twin pairs) — authored by an isolated Opus sub, self-validated 34/34 needles + 0 decomposed sara-am before the run.

## Per-round results (fidelity gate PASS on every round — 0 facts lost)

| Fixture | R1 muscle kept | R1 fat removed | R2 (post-R1 state) |
|---|---|---|---|
| f01-dup-heavy | 8/8 | 2/2 (100%) | fat-exhausted: no-op by adjudication, 0 loss |
| f02-superseded | 7/7 | 3/3 (100%) — incl. a human-gated whole-file delete + its approved index link-drop | fat-exhausted: no-op, 0 loss |
| f03-lean | no-op PASS (lean discipline held) | — (no fat planted) | no-op PASS again — outsider flags rejected-all, twice |
| f04-thai-mixed | 7/7 — composed sara-am (U+0E33) preserved | 1/1 (100%) | fat-exhausted: no-op, 0 loss |
| f05-plump-real-shape | 34/34 | 16/17 (94.1%) | steady-state no-op — the washer + adjudication refused the last fat needle (f10) on merit: fidelity-over-aggressiveness; fat never exhausted on this fixture |

## Measurement 1 — consecutive-run ceiling (fable tier)

No bad round observed through round 2 on any fixture — 0 muscle loss anywhere. **Ceiling ≥ 2 (tested to round 2; no loss observed).** Published exactly as tested — never extrapolated beyond the tested rounds.

## Measurement 2 — infinity-loop fact-loss (fable tier)

Post-exhaustion rounds were a **100% no-op on f01/f02/f04 — 0% fact-loss per round**, the structural target met.

**Qualitative finding (the wedge, observed live):** the post-exhaustion outsider flags TURNED DANGEROUS exactly as the wedge predicts — round-2 flags targeted MUSCLE (dated lesson kernels, version-history chains, index files, Unicode-codepoint rationale). The insider adjudication rejecting those flags is what held loss at 0%. One planted contradiction-candidate (schema-attribution) was correctly flagged-not-touched: unverifiable → no fabricated fix.

## Arm 2 — real-store clone, sealed-probe fidelity (same day)

**Target:** a clone of a real 73-file / 343,366-byte memory store (author≠solver held: the probe set was authored from the PRE-wash text and sealed before any wash). **Apparatus:** 201 sealed probes · 2 blind zero-context answerers (claude-sonnet-5; one on the pristine clone, one on the washed clone; forbidden from the answer key) · a mechanical diff classifier · an independent full-diff out-of-frame reviewer (claude-opus, BEFORE-vs-AFTER, no access to the probe apparatus) · the fable session main as insider/washer.

| Layer | Catches | Class |
|---|---|---|
| mechanical fidelity gate | 3 | structured tokens (a wikilink repoint, 2 version strings) — caught pre-apply, fixed |
| answerer-probe diff (201 sampled) | 2 | sampled semantic (a numeric breakdown + a verbatim quote, both dropped in merge-folds) — fixed |
| out-of-frame full diff (slow, exhaustive) | 15 (1 critical) | exhaustive semantic — incl. the round's worst class: **claim-strengthening** ("all fixed except 2 deferred" compressed to "all fixed") — all restored |

**Results:** LOSS-CANDIDATE (pristine-found / washed-missing) = **0**. Fact-survival **199/201 pre-fix (99.0%) → 201/201 after the catch layers**. Washed-store recall **83.6% ≥ pristine 81.1%** (the wash improved findability slightly; it never degraded it). The three layers caught fully **DISJOINT** loss classes — no layer was redundant.

**The improvement loop this arm drove (shipped as v0.1.0-beta.6):** the 2 sampled losses were exactly the token classes the gate lacked → `quote-drop` + `number-drop` (+ `codespan-drop` from the full-diff layer) are now code-proven gate classes; the claim-strengthening catch became the merge claim-strength-invariance rule + a mandatory post-merge out-of-frame diff; repeat-challenge fatigue became the keep-verdict store; a parallel incident-mining pass (39 documented disk-maintenance disasters, mechanism-level transfer filter) added 5 transactional-apply guards. Engine tests 102 → 148.

## Honest scope

Single model tier measured (fable); multi-tier pending. Small fixture N (5 planted stores + 1 real clone). Rounds capped at 2 by protocol budget this run — the ceiling number is a tested floor, not a limit. Arm-2 caveats: 36/201 probe rows were answerer PHRASING variance (the method's noise floor — both answers correct, worded differently), 4 probes carried a deliberately-reconciled stale value as their sealed expected (the wash corrected the store; the probe shows the correction); the human gate in this run was consent-level, not expert row-adjudication (the analytical out-of-frame seat is an AGENT seat by design). Measurement 3 (sawtooth-vs-bloat, the headline saving) has not run. Engine version tested: v0.1.0-beta.5; the fixes this run drove shipped as v0.1.0-beta.6 the same day.
